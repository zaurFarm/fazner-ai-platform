# 🚀 Простая инструкция по развертыванию и обновлению

## 📋 Что нужно для начала

### Системные требования сервера:
- **RAM:** 4-8 GB
- **CPU:** 2-4 ядра  
- **Storage:** 50+ GB SSD
- **OS:** Ubuntu 20.04+ / CentOS 8+ / Debian 11+
- **Доступы:** SSH + FTP

---

## 🛠️ ПОШАГОВАЯ НАСТРОЙКА СЕРВЕРА

### ШАГ 1: Подготовка сервера (один раз)

**Подключитесь к серверу через SSH:**
```bash
ssh ваш-пользователь@IP-СЕРВЕРА
```

**Установите Docker (выполните по порядку):**
```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установите Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Установите дополнительные пакеты
sudo apt install -y git curl wget unzip htop nano

# Перезайдите в систему (важно!)
exit
# Затем снова: ssh ваш-пользователь@IP-СЕРВЕРА
```

### ШАГ 2: Настройка FTP (один раз)

**Установите FTP сервер:**
```bash
sudo apt install -y vsftpd
sudo systemctl enable vsftpd
sudo systemctl start vsftpd
```

**Создайте пользователя для FTP:**
```bash
sudo useradd -m -s /bin/bash minimax
sudo passwd minimax
# Введите пароль дважды

# Создайте папку для проекта
sudo mkdir -p /home/minimax/fazner-ai-platform
sudo chown minimax:minimax /home/minimax/fazner-ai-platform
```

### ШАГ 3: Загрузка проекта

**Через FTP клиент (FileZilla, WinSCP):**
- **Хост:** IP вашего сервера
- **Порт:** 21
- **Пользователь:** minimax
- **Пароль:** тот что установили
- **Протокол:** FTP

**Загрузите все файлы проекта** в папку `/home/minimax/fazner-ai-platform/`

### ШАГ 4: Настройка и запуск

**Подключитесь к серверу:**
```bash
ssh minimax@IP-СЕРВЕРА
cd /home/minimax/fazner-ai-platform
```

**Настройте переменные окружения:**
```bash
# Скопируйте пример файла
cp .env.example .env

# Отредактируйте файл
nano .env
```

**В файле .env укажите:**
```env
OPENROUTER_API_KEY=ваш-openrouter-api-ключ
JWT_SECRET=сгенерированный-секрет
SESSION_SECRET=сгенерированный-секрет
ADMIN_EMAIL=ваш-email@домен.com
ADMIN_PASSWORD=безопасный-пароль
```

**Сделайте скрипты исполняемыми:**
```bash
chmod +x deploy.sh
chmod +x server-scripts/*.sh
```

**Запустите платформу:**
```bash
./deploy.sh deploy
```

---

## 🔄 КАК ОБНОВЛЯТЬ САЙТ

### Метод 1: Через FTP (самый простой)

1. **Загрузите новые файлы** через FTP в папку `/home/minimax/fazner-ai-platform/`

2. **Подключитесь к серверу:**
```bash
ssh minimax@IP-СЕРВЕРА
cd /home/minimax/fazner-ai-platform
```

3. **Запустите обновление:**
```bash
./server-scripts/ftp-update.sh
```

4. **Выберите пункт 1** (Полное обновление)

5. **Подождите** 2-3 минуты

6. **Проверьте результат** - сайт обновлен!

### Метод 2: Через Git (если настроен)

```bash
cd /home/minimax/fazner-ai-platform
git pull origin main
./server-scripts/update.sh
```

---

## 📊 КОМАНДЫ ДЛЯ УПРАВЛЕНИЯ

### Основные команды:
```bash
# Обновление через FTP
./server-scripts/ftp-update.sh

# Полное обновление
./server-scripts/update.sh

# Мониторинг системы
./server-scripts/monitor.sh

# Просмотр логов
docker-compose logs -f

# Статус сервисов
docker-compose ps

# Перезапуск всех сервисов
docker-compose restart

# Остановка всех сервисов
docker-compose down

# Запуск всех сервисов
docker-compose up -d
```

### Быстрые проверки:
```bash
# Проверка доступности сайта
curl -I http://localhost:3000

# Проверка API
curl http://localhost:5000/health

# Проверка базы данных
docker-compose exec postgres pg_isready -U minimax_user -d minimax_ai_platform

# Проверка Redis
docker-compose exec redis redis-cli ping
```

---

## 🌐 ДОСТУП К САЙТУ

После успешного развертывания:

**Ваш сайт будет доступен по адресам:**
- **Сайт:** http://IP-СЕРВЕРА:3000
- **API:** http://IP-СЕРВЕРА:5000
- **Проверка работы:** http://IP-СЕРВЕРА:5000/health

**Чтобы узнать IP вашего сервера:**
```bash
curl -s ifconfig.me
```

---

## 🛠️ УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: Сайт не открывается
```bash
# Проверьте статус контейнеров
docker-compose ps

# Проверьте логи
docker-compose logs

# Перезапустите сервисы
docker-compose restart
```

### Проблема: Ошибки при обновлении
```bash
# Остановите и очистите все
docker-compose down -v
docker system prune -af

# Запустите заново
./server-scripts/update.sh
```

### Проблема: Недостаточно места
```bash
# Очистите Docker
docker system prune -af

# Очистите логи системы
sudo journalctl --vacuum-time=7d

# Проверьте использование диска
df -h
```

### Проблема: FTP не работает
```bash
# Проверьте статус vsftpd
sudo systemctl status vsftpd

# Перезапустите FTP
sudo systemctl restart vsftpd

# Проверьте логи
sudo tail -f /var/log/vsftpd.log
```

---

## 💡 ПОЛЕЗНЫЕ СОВЕТЫ

### 1. Регулярные бэкапы:
```bash
# Создайте бэкап
cp -r . backups/backup-$(date +%Y%m%d)

# Восстановите из бэкапа
cp -r backups/backup-20241212 .
./server-scripts/update.sh
```

### 2. Мониторинг ресурсов:
```bash
# Посмотрите использование ресурсов
./server-scripts/monitor.sh

# Или через htop
htop

# Или df -h для диска
df -h
```

### 3. Обновление только одного сервиса:
```bash
# Только бэкенд
docker-compose build backend
docker-compose up -d backend

# Только фронтенд
docker-compose build frontend
docker-compose up -d frontend
```

### 4. Просмотр логов конкретного сервиса:
```bash
# Логи бэкенда
docker-compose logs -f backend

# Логи фронтенда
docker-compose logs -f frontend

# Логи базы данных
docker-compose logs -f postgres
```

---

## 🎯 ИТОГОВАЯ СХЕМА

```
1. Подготовка сервера (один раз)
   ↓
2. Загрузка проекта через FTP
   ↓
3. Запуск: ./deploy.sh deploy
   ↓
4. Обновление: ./server-scripts/ftp-update.sh
   ↓
5. Готово! Сайт работает на IP-СЕРВЕРА:3000
```

**🎉 Все готово! Теперь у вас есть полностью рабочая AI-платформа на собственном сервере!**